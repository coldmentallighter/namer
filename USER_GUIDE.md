# File Namer 完整使用指南

File Namer 是一款 Windows 本地离线批量文件命名与文件名表格导出工具。它只修改文件名，不移动文件，不修改文件内容，也不改变目录结构。软件通过本机 WebUI 工作，服务只绑定到 `127.0.0.1`，扫描、重命名、XLSX 读写和历史记录都在本机完成。

## 1. 运行方式

### 1.1 使用可分发版本

在 `dist` 文件夹中：

1. 双击 `start_webui.bat`，或直接运行 `OfflineFileNamer.exe`。
2. 程序会启动本机服务并打开默认浏览器。
3. 如果浏览器没有自动打开，使用终端输出的 `http://127.0.0.1:<端口>/` 地址访问。
4. 历史记录保存在 EXE 同级的 `history\\history.json`。

关闭 WebUI 标签页后，程序会在几秒内自动退出；刷新页面会保留本地服务。

可分发版本不需要目标电脑安装 Python 或 openpyxl。建议分发时保持以下文件在同一个文件夹：

```text
OfflineFileNamer.exe
start_webui.bat
README.txt
history\\
```

### 1.2 从源代码运行

在项目根目录执行 `install_environment.bat`，它会创建 `.venv` 并安装依赖。安装完成后双击 `start_webui.bat`，或者执行：

```powershell
python main.py
```

源代码运行时历史记录位于项目根目录的 `history\\history.json`。首次成功重命名时自动创建 `history` 文件夹。

### 1.3 运行条件

- Windows 10/11，推荐 64 位系统。
- 通常少于 1000 个文件的单次任务。
- 完全离线运行，不需要网络服务或云账号。
- 默认浏览器需要允许访问 `127.0.0.1`。

## 2. 界面总览

| 区域 | 用途 |
| --- | --- |
| 顶部栏 | 输入或选择根目录、扫描、切换深色模式 |
| 左侧栏 | 查看命名组、切换组是否参与全部执行、设置隐藏/系统文件范围、撤销和还原 |
| 批量命名页 | 设置命名规则、筛选扩展名、编辑文件列表、导入 XLSX、执行重命名 |
| 导出文件名表格页 | 扫描并按文件夹导出 XLSX |
| 底部日志栏 | 查看 INFO/WARN/ERROR 日志，可折叠并拖动高度 |
| 音频预览条 | 播放支持的音频、拖动进度和调节音量 |

所有文件操作都针对本地磁盘执行。浏览器页面关闭不会删除任务文件，也不会自动撤销已经完成的重命名。

## 3. 第一次扫描

### 3.1 选择根目录

在顶部“根目录”输入 Windows 路径，例如：

```text
D:\\Samples\\EDM Pack
```

也可以点击“选择”打开 Windows 原生文件夹选择器。选择完成后点击“扫描”。

当前版本不支持直接把资源管理器中的文件夹拖到浏览器页面后自动读取绝对路径；请使用原生选择器或手动输入路径。

### 3.2 扫描范围

左侧有两个范围开关：

- “包含隐藏文件”：包含 Windows 隐藏文件和名称以点号开头的文件/目录。
- “包含系统文件”：包含 Windows 系统属性文件。

修改开关后需要再次点击“扫描”应用范围；范围或根目录与当前扫描结果不一致时，预览和重命名按钮会暂停使用，避免误操作旧目录。已有名称、选择或 Excel 映射时，重新扫描前会要求确认。扫描会递归遍历根目录及所有子目录，目录和文件名按自然排序处理，例如 `file2` 会排在 `file10` 前面。

软件会忽略自身生成的表格：

- `*.ffnf.xlsx`
- `*.ori01.ffnf.xlsx`
- `*.ori02.ffnf.xlsx`
- 以及其他 `.ori` 数字编号表格

这些表格不会计入扩展名统计，也不会再次参与命名或导出。

### 3.3 扫描结果

扫描完成后，顶部摘要会显示：

- 文件：扫描到的文件总数。
- 扩展名：实际发现的扩展名数量。
- 当前组：当前选中的命名组。

扩展名统一按大小写不敏感方式统计，例如 `.WAV` 和 `.wav` 属于同一类，但执行重命名时会保留每个文件原本的扩展名大小写。

## 4. 命名组

命名组按“文件所在目录 + 扩展名”划分；界面同时显示从根目录开始的完整相对路径，避免多层目录或同名末端目录混淆。例如：

```text
Drums&Loop\\Kick 01.wav  -> Drums&Loop / WAV
Drums&Loop\\Snare 01.wav -> Drums&Loop / WAV
MIDI\\Pattern 01.mid     -> MIDI / MID
```

即使两个目录的末端名称相同，只要相对路径不同，也不会合并为同一组。不同命名组之间完全隔离：

- 不混用 Excel 匹配关系。
- 不共享数字编号。
- 不共享组前缀配置。
- 某组有冲突时，不会阻止其他无冲突组执行。

左侧每一行命名组包含一个开关和组名称：

- 点击组名称：切换当前编辑的命名组。
- 点击组前面的勾选符号：切换该组是否参加“执行已选组”。

命名组数量很多时，左侧列表会独立滚动，不会把主页面无限撑长。左侧栏与主内容之间的分隔条可以水平拖动调整宽度，宽度会保存在浏览器本地设置中。

## 5. 批量命名页

### 5.1 元前缀、组前缀和子前缀

最终文件名由以下非空字段按分隔符连接：

```text
元前缀 + 分隔符 + 组前缀 + 分隔符 + 子前缀 + 分隔符 + 名称 + 原扩展名
```

默认值采用方案一：取文件所在目录路径末端的三个目录名称（根目录也参与浅层路径的回退）。例如 `Root\\A\\B\\C` 中，元前缀、组前缀、子前缀依次为 `A`、`B`、`C`；`Root\\A\\B` 回退为 `Root`、`A`、`B`；`Root\\A` 回退为 `Root`、`A`、空；根目录直接包含的文件使用 `Root`、空、空。各组的默认元前缀和组前缀独立保存。

用户修改元前缀后，该值作为共享覆盖值应用到后续执行的组，仍可通过组前缀和子前缀分别细调。

- 元前缀：默认来自末端三级规则，原样保留大小写、空格和特殊字符。
- 组前缀：默认来自末端三级规则中的中间目录。
- 子前缀：默认来自末端三级规则中的末级目录；浅层路径为空。
- 名称：原文件名（不含扩展名）。
- 分隔符：`_`。

空字段会被跳过，不会产生连续或多余分隔符。例如：

```text
元前缀 = Pack
组前缀 = Drums&Loop
子前缀 = 空
名称   = Chest
扩展名 = .wav

结果：Pack_Drums&Loop_Chest.wav
```

合法的中文、英文、空格、`#`、`&`、`-`、`_` 等字符会原样保留。修改输入后点击“刷新预览”，或离开输入框触发自动刷新。

### 5.2 参与处理扩展名

扫描完成后，“参与处理”区域会动态显示实际发现的扩展名和数量。它不是固定格式列表，扫描到的任何扩展名都可以参与处理。

- 勾选：该扩展名的文件可以被预览、编号和重命名。
- 取消勾选：对应文件不参与当前命名任务，也不占用数字编号。
- 重新勾选并刷新预览：软件会自动恢复此前因该扩展名被取消而跳过的文件勾选；用户手动取消的文件不会被自动选中。

扩展名过滤和命名组是两个层次：扩展名决定哪些文件进入任务，命名组决定这些文件之间如何隔离配置和编号。

### 5.3 名称模式

“名称模式”有三种选择：

#### 原文件名

使用原文件名（不含扩展名）作为“名称”字段。只要前缀或分隔符发生变化，就可以批量生成新的完整文件名。

#### 数字编号

每个命名组独立编号。可设置：

- 起始：默认 `1`。
- 位数：默认 `2`，所以第一个编号为 `01`。
- 步长：默认 `1`。

编号顺序默认使用自然排序，也可以在表格中拖动行调整顺序。以下文件不会占用编号：

- 未勾选的文件。
- 未匹配 Excel 的文件。
- `Skipped`、`Conflict`、`Error` 等异常状态文件。

数字编号使用独立的派生名称，原扩展名和基础名称始终保留；切回“原文件名”会恢复进入数字模式前的名称。

#### Excel 名称

导入 XLSX 后软件会自动切换为 Excel 名称模式。Excel 匹配成功的值会作为派生“名称”字段，元前缀、组前缀、子前缀和扩展名仍按当前配置组合；切回“原文件名”会恢复基础名称。没有 Excel 映射且不能从关联文件继承名称的组不会显示为可执行组。

音频和 MIDI 命名组可以点击“添加 BPM 后缀”。软件优先读取文件内部的 BPM/tempo metadata，读取不到时再从文件名识别；后缀格式为 `<名称>_<BPM>BPM`，重复点击不会重复添加。无法识别 BPM 的文件会保留原名称并在提示中列出。

### 5.4 文件列表

文件表格每行包含：

- 选择框。
- 当前文件名。
- 当前文件夹名称/相对路径。
- 扩展名。
- 子前缀。
- 名称。
- 新文件名预览。
- 状态和状态详情。
- 音频播放按钮（音频文件才显示）。
- “单项”重命名按钮。
- “移除”按钮。

常用操作：

- 搜索框：按当前文件名或目标文件名过滤。
- 状态筛选：只看 Ready、Renamed、Conflict、Skipped、未匹配或 Error。
- “全选可见”：只选中当前搜索和状态过滤后可见的行。
- “取消选择”：取消当前可见行的选择。
- “移除选中”：从当前任务移除文件行，不删除磁盘文件。
- 编辑“子前缀”或“名称”：修改单行字段并实时更新预览。
- 拖动整行：调整当前命名组的处理顺序。

状态含义：

| 状态 | 含义 |
| --- | --- |
| Ready | 已选择，预览有效，等待执行 |
| Renamed | 已成功重命名 |
| Unchanged | 目标文件名与当前文件名相同，无需移动文件 |
| Conflict | 预览或执行前检查发现冲突/非法名称 |
| Skipped | 被取消选择、Excel B 列为空或被移除 |
| 未匹配 | 没有匹配到 Excel 名称 |
| Error | 执行时发生文件系统错误 |

## 6. 音频预览

当前音频预览扩展名包括：`.wav`、`.wv`、`.mp3`、`.aif`、`.aiff`、`.flac`、`.aac`、`.ogg`、`.m4a`。

在文件表格中点击音频行的播放按钮，底部音频条会显示：

- 播放/暂停按钮。
- 当前播放时间。
- 可拖动的自定义进度滑块。
- 总时长。
- 音量滑块。

长音频支持 HTTP Range 分段读取，拖动滑块时不需要等待整个文件下载完成。浏览器对 `.wv`、部分 `.aif/.aiff`、`.aac` 等格式的解码能力可能不同；如果浏览器不支持解码，会显示错误提示，但不会影响文件扫描和重命名。

## 7. XLSX 导入

### 7.1 导入步骤

1. 先扫描根目录。
2. 在左侧选择要导入的命名组。
3. 点击摘要栏中的“导入 XLSX”。
4. 选择 `.xlsx` 文件。
5. 软件读取第一个工作表，并在当前命名组内匹配。
6. 查看表格中的名称、状态和底部日志。
7. 确认预览无误后执行重命名。

Excel 中的文件名可以带扩展名，也可以不带扩展名。匹配时会忽略扩展名差异、路径前缀和大小写差异。例如 `D:\\Input\\Kick 01.wav` 可以匹配 `Kick 01` 或 `Kick 01.wav`。

### 7.2 模式一：A 列源文件名，B 列新名称

当工作表存在 B 列时：

| A 列 | B 列 | 结果 |
| --- | --- | --- |
| 源文件名 | 新名称 | 匹配后使用 B 列作为名称 |
| 源文件名 | 空 | 对应文件不改动，并记录 WARN |
| 不存在的文件名 | 任意 | Excel 行不执行，并记录 WARN |

A 列优先按源文件名匹配，不按 Excel 行号强制对应。实际文件没有匹配到 Excel 行时会显示“未匹配”，不会改动，也不会参与数字编号。

### 7.3 模式二：只有 A 列名称

当 Excel 没有 B 列时，A 列每行都视为新名称，并按当前命名组的文件列表顺序分配：

- Excel 行数少于文件数：多出的文件显示“未匹配”，不改动。
- Excel 行数多于文件数：多余行记录 WARN。
- A 列为空：视为无效名称，记录 WARN。

导入完成后，顶部提示和日志会给出匹配成功数、未匹配文件数和未匹配行数。

## 8. 执行重命名

执行前建议按以下顺序操作：

1. 先扫描并确认根目录正确。
2. 选择参与处理的扩展名。
3. 选择当前命名组，配置前缀、分隔符和名称模式。
4. 点击“刷新预览”。
5. 检查每行新文件名和状态。
6. 先执行一个小命名组验证结果。
7. 确认无误后再执行全部已选组。

按钮区别：

- “重命名当前组”：只执行当前正在查看的命名组。
- “执行已选组”：执行左侧已启用的所有命名组；每组独立检查、独立编号和独立写入结果。
- 行内“单项”：只执行当前这一行文件。

### 8.1 单项命名的用途

单项命名适合先试改一个文件，或在批量任务完成后补改某一个文件。它会使用和批量重命名相同的检查：

- 非法字符和保留设备名。
- 目标文件已存在。
- 本次目标重复。
- 源文件不存在。
- 文件权限、占用和读取失败。
- 路径过长。

单项命名不会影响同组其他文件。成功后会写入历史记录，也可以通过“撤销最近操作”撤销。

## 9. 冲突和错误处理

软件执行前会检查：

- 目标文件已经存在。
- 本次任务内部生成重复目标名。
- Windows 非法字符：`\\ / : * ? " < > |`。
- 控制字符。
- 文件名以空格或点结尾。
- Windows 保留设备名，例如 `CON`、`PRN`、`AUX`、`NUL`、`COM1`、`LPT1`。
- 源文件不存在。
- 没有权限、文件被占用或无法读取。
- 路径或文件名超过 Windows 传统长度限制。

默认策略是“阻止而不是静默修改”：软件不会自动替换非法字符，不会覆盖已有文件，也不会删除文件。

如果一个命名组存在预检冲突，该组的执行会被阻止；执行全部已选组时，其他无冲突组仍可以继续。冲突行会高亮，状态显示为 `Conflict`，日志写入 `ERROR`。

## 10. 日志和历史记录

### 10.1 日志

底部“运行日志”栏记录：

- 扫描结果。
- Excel 匹配结果。
- 未匹配文件和未匹配行。
- 冲突和非法名称。
- 单项、当前组和批量重命名结果。
- 导出 XLSX 结果。
- 跳过原因和失败原因。

日志级别：

- `INFO`：正常流程和成功信息。
- `WARN`：未匹配、空 Excel 行、跳过等非致命问题。
- `ERROR`：冲突、权限失败、文件系统错误等。

点击日志标题可以折叠/展开。拖动日志顶部的横向把手可以调整高度，设置会保存在当前浏览器中。

撤销和还原成功时，`INFO` 会显示本次实际处理的命名组和文件名；跨组批量操作会列出多个组，文件很多时显示前 8 个文件并注明总数。部分成功时仍会写入 INFO，同时对失败项目分别写入 `ERROR`。

日志主要用于当前运行过程查看，不作为跨重启的完整审计文件。

### 10.2 历史记录和撤销

每次有成功文件的重命名操作都会写入 `history\\history.json`，至少包含：

- 操作时间。
- 原始完整路径。
- 新完整路径。
- 命名组标识。
- 成功/失败状态。
- 错误信息。

点击左侧“撤销最近操作”时，软件会检查：

- 预期路径上的文件指纹是否仍与历史一致。
- 路径失效时，原目录内是否存在唯一的文件 ID、SHA-256 和大小匹配项。
- 原路径是否仍然空闲，整批文件是否都可重命名。

只有仍符合条件的文件会被恢复。无法恢复的项目会记录 `ERROR`。历史记录不会因为撤销而删除，而是保留撤销时间和恢复数量。

如果用户在重命名后于同一目录内再次改名，软件会在预期路径失效时按文件指纹查找唯一候选，再执行撤销。移动到其他目录、删除、修改内容，或目录内出现多个无法区分的相同候选时会安全拒绝。

点击其下方的“还原最近撤销”可以重新执行最近一次已经完整撤销的重命名操作。还原同样通过文件指纹确认或重新定位源文件，并统一检查全部目标路径；目标已被其他文件占用、源文件身份不符或权限不足时不会覆盖或删除任何文件，并在日志中记录 `ERROR`。跨命名组的“执行已选组”会作为一个整体还原。还原完成后，历史记录会保留 `redone_at` 标记，之后仍可再次点击“撤销最近操作”回滚这次还原。

撤销和还原会先解析整批文件并统一预检查，再通过临时文件名提交；任一项目预检查失败时整批不执行，运行阶段失败时尝试整体回滚。只有底层文件系统同时阻止提交和回滚时才可能出现部分回滚，并会明确记录 `ERROR`。

## 11. 导出文件名表格

导出模块与批量命名模块相互独立，用于为每个内容文件夹生成可再次用于命名导入的 XLSX。“刷新扫描”只刷新导出范围和扩展名，不会清空命名页正在编辑的名称、选择或 Excel 映射。

### 11.1 导出步骤

1. 在顶部选择或输入根目录。
2. 点击“导出文件名表格”标签页。
3. 选择是否包含隐藏文件、系统文件。
4. 点击“刷新扫描”。
5. 在“导出扩展名”中勾选需要导出的扩展名。
6. 点击“导出并打开根目录”。

导出完成后，软件会自动打开根目录资源管理器，并在结果区域列出生成的 XLSX 路径。若根目录下存在三级或更多层的嵌套目录，还会同时生成根目录下的 `filetree.txt` 表格索引文件。

### 11.2 导出范围和文件夹规则

- 扫描根目录及所有子目录。
- 只有直接包含被选扩展名文件的文件夹才生成表格。
- 空文件夹不生成表格。
- 表格写入内容文件夹的父级目录。
- 不会覆盖已有表格。
- 所有软件生成的 `.ffnf.xlsx` 表格都会自动忽略。
- `filetree.txt` 使用 UTF-8 和稳定的自然排序，只记录本次实际生成 `.ffnf.xlsx` 的内容目录及对应 XLSX 路径，不列出目录中的全部文件；文件已存在时不会覆盖，新的导出会跳过该文件并保留原内容。旧版本名称 `Structure.ffnf.txt` 仍会记录完整目录树并自动忽略。

例如，内容文件夹为：

```text
D:\\Pack\\Drums
```

生成的表格默认写入：

```text
D:\\Pack\\Drums.ffnf.xlsx
```

### 11.3 工作簿结构

一个文件夹生成一个工作簿，一个工作簿按扩展名拆分工作表：

- 工作表名称使用扩展名去掉点号后的名称，通常转为大写，例如 `WAV`、`PNG`。
- 每个工作表第一列写入不含扩展名的文件名。
- 文件名按自然排序。
- 没有文件的扩展名不会生成空工作表。
- 表格内容可以直接作为后续批量命名的 Excel 名称输入。

### 11.4 输出文件名冲突

如果默认文件名已经存在，按以下顺序生成：

```text
Folder.ffnf.xlsx
Folder.ori01.ffnf.xlsx
Folder.ori02.ffnf.xlsx
Folder.ori03.ffnf.xlsx
...
Folder.ori100.ffnf.xlsx
```

`.ori` 编号从 `01` 开始，超过 `99` 后继续使用 `100`、`101` 等，不会覆盖任何旧表格。

### 11.5 XLSX 导出

导出页统一生成详细 XLSX。文件名为 `<目录>.ffnf.xlsx`，冲突时依次使用 `<目录>.ori01.ffnf.xlsx`、`<目录>.ori02.ffnf.xlsx` 等。扩展名工作表包含 `SourceName`、`NewName`、相对路径、大小、修改时间、同 stem 关联信息、`BPM` 和空白 `Scale` 列，并增加 `Metadata`、`Summary` 工作表。

详细表格用于重命名时，只编辑对应扩展名工作表的 B 列 `NewName`；A 列 `SourceName` 用于在当前命名组内精确匹配。B 列可以使用 `{bpm}` 和 `{scale}`，例如 `Pad_{scale}_{bpm}`；`BPM` 列默认由文件 metadata/名称识别，`Scale` 列留空供手动填写。空的占位字段会被移除，B 列为空或展开后为空的行会跳过。导入时软件会按当前组扩展名自动选择工作表，不依赖工作表排列顺序。`Metadata` 和 `Summary` 仅供查看，不作为名称输入。

导出结果区域会显示总目录数、文件数、内容目录数和空目录数。导出会自动排除所有 `.ffnf.xlsx` 和 `.ori` 变体。

## 12. 目录层级映射、解析与跨格式关联

### 12.1 目录层级映射

命名页的“目录层级映射”允许把任意目录级别指定为元前缀、组前缀和子前缀：

- `0` 表示根目录；`1` 表示根目录下第一级；以此类推。
- `-1` 表示当前文件夹；`-2` 表示上一级；`-3` 表示上两级。
- “不使用”表示该字段为空。
- 默认预设仍为末端三级目录映射，浅层目录保持原有回退规则。

点击“应用映射”后会更新所有命名组的默认前缀；用户仍可以在当前组中手动覆盖元前缀、组前缀或子前缀。

### 12.2 文件名解析模板

解析模板用于识别文件名中的结构化字段，并提供解析预览。模板示例：

```text
{type}_{name}_{number}_{bpm}
```

支持字段 `{name}`、`{type}`、`{number}`、`{bpm}`、`{scale}`、`{key}`、`{category}`。模板留空或填写 `auto` 时，软件会尝试识别 BPM、调性和末尾编号。解析结果、未匹配内容和置信度显示在文件表格的“解析结果”列。Excel B 列也支持 `{bpm}` 和 `{scale}`，其值优先使用表格中的 `BPM`/`Scale` 列。

解析默认只用于预览；勾选“将解析出的 name 用作名称”后，解析出的 `name` 才会填入名称字段并参与目标文件名生成。解析不会修改文件内容或扩展名。

### 12.3 跨格式同 stem 关联

同一目录中去掉扩展名后相同的文件会显示关联标签，例如 `Kick_01.wav`、`Kick_01.mid` 会显示 `WAV/MID` 关联。WAV、MID 等仍属于不同命名组，但对其中一个文件编辑名称、解析、添加 BPM 后缀或导入 Excel 后，该文件会成为关联名称来源；切换到其他格式刷新预览不会反向覆盖它。符合条件的关联文件会继承相同的目标 stem，并在同一次重命名事务中执行和撤销。被取消选择、移除或禁用扩展名的关联文件不会被自动加入；Excel 中未匹配的关联文件可继承已匹配文件的目标 stem。

## 13. 事务式重命名与安全撤销

每个命名组执行时会先完成统一预检查，再使用临时文件名进行阶段改名，最后提交目标名称。执行阶段发生异常时，软件会尝试把已经完成的文件恢复到原路径，并在日志中报告“已回滚”或“部分回滚”。不同命名组相互独立。

历史记录的每个成功项目都保存文件大小、修改时间、文件 ID 和 SHA-256 指纹。撤销或还原时路径只作为快速定位提示：预期路径不存在或身份不符时，会在原目录按指纹寻找唯一文件。文件被替换、内容发生变化或出现多个候选时会拒绝触碰并记录 `ERROR`。旧版本没有指纹的历史仍可按原路径规则兼容撤销，但不会获得重新定位保护。

## 14. 深色模式和界面操作

点击右上角“深色/浅色”按钮切换主题。主题选择会保存在浏览器本地，下次打开 WebUI 会恢复上次选择。

深色模式覆盖：

- 页面背景和面板。
- 文件表格和表格容器。
- 文件名、路径、扩展名和状态标签颜色。
- 页面、命名组和表格内部滚动条。
- 日志栏和输入控件。

主题切换带有短暂过渡动画，不会改变文件操作数据。

## 15. 推荐工作流

### 13.1 普通批量命名

```text
选择根目录
  -> 扫描
  -> 选择扩展名
  -> 选择命名组
  -> 配置前缀/分隔符/名称模式
  -> 刷新预览
  -> 检查冲突
  -> 执行当前组或执行已选组
```

### 13.2 数字编号

```text
扫描
  -> 选择命名组
  -> 名称模式 = 数字编号
  -> 设置起始、位数、步长
  -> 取消不需要的文件
  -> 拖动调整顺序
  -> 刷新预览
  -> 执行
```

### 13.3 Excel 命名

```text
扫描
  -> 选择命名组
  -> 导入 XLSX
  -> 检查匹配数量和 WARN
  -> 检查未匹配行/文件
  -> 刷新预览
  -> 执行
```

### 13.4 导出后再导入

```text
导出文件名表格
  -> 得到 Folder.ffnf.xlsx
  -> 在批量命名页选择同一命名组
  -> 导入该 XLSX
  -> 检查名称和匹配关系
  -> 执行命名
```

## 16. 常见问题

### Q1：点击“选择”没有弹出窗口

请确认使用的是最新 `dist\\OfflineFileNamer.exe`。当前可分发版本使用 Windows 原生选择器，不需要 Tkinter。旧版本如果提示 `No module named 'tkinter'`，需要用新 EXE 替换。

### Q2：音频能扫描但不能播放

扫描和播放是两个独立功能。浏览器可能不支持某些 `.wv`、`.aif/.aiff`、`.aac` 编码。可以换用支持该格式的 Windows 浏览器；文件仍然可以作为普通文件重命名。

### Q3：目标文件名显示 Conflict

查看该行状态详情和日志。常见原因是目标已存在、目标重复、非法字符、保留设备名、文件被占用或路径过长。修正名称或取消该行后刷新预览。

### Q4：为什么文件没有编号？

只有已选择且状态正常的文件参与编号。取消选择、Excel 未匹配、空名称、Conflict、Skipped、Error 文件都不会占用编号。

### Q5：为什么 Excel 行没有执行？

先确认当前选择的命名组是否正确，再检查 A 列源文件名、扩展名和工作表是否正确。模式一按源文件名匹配；模式二按当前组文件顺序匹配。所有异常都会写入 WARN 日志。

### Q6：导出后再次扫描仍然看到旧表格吗？

软件会自动忽略匹配 `*.ffnf.xlsx` 和 `.oriNN.ffnf.xlsx` 的自生成表格。如果文件名不符合该规则，则会被视为普通 XLSX 文件。

### Q7：撤销失败怎么办？

先查看日志中的具体原因。同目录手动改名通常可由指纹重新定位；移动到其他目录、删除或修改文件内容无法自动恢复。如果原路径被其他文件占用或指纹出现多个候选，软件会保持整批文件不变并记录 ERROR。

### Q8：导出按钮没有生成表格

确认已刷新扫描、至少勾选一种扩展名，并确认文件夹中存在符合范围条件的文件。空文件夹和没有被选扩展名的文件夹不会生成表格。

## 15. 安全建议

批量重命名前建议：

1. 先对少量文件夹进行试运行。
2. 先用“原文件名”或小范围数字编号检查预览。
3. 确认没有 `Conflict`、`未匹配` 或 `Error` 行。
4. 对重要素材先备份目录。
5. 执行后不要立即在资源管理器中再次批量改名，避免影响撤销条件。

软件不会覆盖目标文件，不会删除源文件，也不会移动文件；但是重命名本身仍然会改变目录中的文件名，因此重要数据应保留备份。

## 16. 构建与测试

源码环境安装：

```powershell
install_environment.bat
```

运行测试：

```powershell
python -m unittest discover -s tests -v
```

构建 Windows 单文件版本：

```powershell
python -m PyInstaller --clean --noconfirm OfflineFileNamer.spec
```

构建结果位于 `dist`；spec 会自动使用 `Artwork\\Logo.ico`，并写入版本 `1.0`、作者 `ColdLightVibe` 元数据。当前测试覆盖扫描、动态扩展名、自然排序、中文和特殊字符、数字编号、冲突阻止、单项/批量重命名、撤销与还原（含跨组、冲突不覆盖和再次撤销）、Excel 两种匹配模式、隐藏/系统文件、XLSX 导出冲突、音频 Range 请求、WebUI API 和 Windows 原生选择器分支。
