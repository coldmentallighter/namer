# 采样包工作流重构纲要

## 1. 文档目的

本纲要根据三个真实采样包目录的文件名和目录结构，重新定义 OfflineFileNamer 的采样包工作流边界。

本次观察的样本包括：

- `[EDM] ColdLight's_IrisEdge_Sample_Pack`
- `Shaw_Tyan 2000 Sounds`
- `Famous Audio - Botanica`

`workflowgenerator.md` 只规定工作流配置的访谈和生成流程，不等于采样包命名规则。本纲要记录的是样本证据和重构建议，不直接覆盖现有 `sample-pack` 工作流。

## 2. 样本证据

### 2.1 ColdLight / IrisEdge 样本

音频和 MIDI 使用两段式前缀：

```text
CL_IE_
```

其中 `CL` 是作者简写，`IE` 是采样包名简写。资源类型位于此前缀之后，例如 `Drums`、`Drone`、`Midi`、`Vocal`、`Texture`、`OneShot`。

典型结构：

```text
作者简写_包名简写_资源类型_子类型_名称_可选变体_可选调式或和弦_可选BPM
```

实际文件中同时存在局部变体编号、完整和弦（如 `Dm7`、`Em7-A7`）和只对部分资源出现的 BPM。预设、Artwork 和说明文件不使用同一套音频前缀。

### 2.2 Shaw / Tyan 样本

音频使用：

```text
Shaw_Tyan_
```

其中 `Shaw` 是作者简写，`Tyan` 是采样包名简写。

`BASS` 类别使用 `01` 到 `48` 的连续资产编号；其他类别通常没有这一层编号。调式或和弦可能是单个音、双音、括号标记或转位表达，例如 `A#_F`、`C(Cm)`、`Cm_over_D`。BPM 有时位于名称和调式之间，例如 `..._130_A`。

### 2.3 Botanica 样本

该包的文件名不应自动补入厂牌名或作者全称。文件名常见结构为：

```text
BT_..._FA
```

根据当前确认，`BT` 是包名相关的简写；`FA` 是作者字段放在后缀位置的个案。这个后缀位置不应提升为所有采样包的通用规则，也不应据此修改默认工作流。

Botanica 中常见的资源结构包括：

```text
BT_Atmos_100_B_Solace_FA
BT_Syn_Bass_100_C_Traveller_FA
BT_Key_100_B_Grainspacer_v2_FA
BT_Syn_Melodic_130_Eb_Unifauna_v1_FA
BT_Found_Sound_Swell_Birdsong_v1_FA
BT_Impact_Tonal_Eb_Exponent_v1_FA
```

它展示了以下事实：作者字段可以缺省或不出现在前缀中；BPM、调式、名称、`v1/v2/v3` 变体和固定尾标都是独立可选段；`Loops`、`Oneshots` 等交付类别主要由目录表达。

## 3. 共同结论

### 3.1 采样包不是单一模板

不能继续假定所有文件都使用：

```text
作者/厂牌_包名_大类别_小类别_名称_编号_调式_BPM
```

更准确的抽象是：

```text
身份字段 + 资源 profile + 可选音乐字段 + 可选变体/资产编号
```

其中身份字段和各段的位置由资源 profile 决定。

### 3.2 身份字段必须拆分且可缺省

建议字段：

```text
author_code?      作者简写，可为空
pack_code         包名简写
author_full?      作者全称，仅作元数据
pack_name_full?   包名全称，仅作元数据
```

作者简写可能位于前缀，也可能是某个包的固定后缀；不能把它固定命名为 `meta_prefix` 并假设位置不变。

### 3.3 资源类型需要分层

建议区分：

```text
delivery_type     Loops / Oneshots 等目录交付类别
resource_type     Drums / Atmos / Synth / Vocal 等资源类型
resource_subtype  Full / Bass / Melodic / Tonal 等子类型
qualifier         GUI / Granular / SangXian 等附加限定词
name              语义名称
```

目录名和文件名 token 可能不完全一致，需要可配置映射。

### 3.4 编号必须分成不同语义

至少需要：

```text
asset_index       类别内连续资产编号，例如 BASS_01
variant            同一语义名称的 v1/v2 或局部 _1/_2
collision_suffix   仅用于解决目标文件名冲突
```

三者不能继续共用当前的 `number` 字段。

### 3.5 音乐字段必须原样保留

调式、和弦、双音、括号和转位表达应进入一个宽泛的原始字段，例如：

```text
key_or_chord
```

其值可以是 `Bbm`、`BbMaj7Add11`、`A#_F`、`C(Cm)` 或 `Cm_over_D`。该字段不应被简单正则拆成 `scale + number`。

### 3.6 BPM 不是固定的末尾后缀

BPM 可能：

- 位于名称之前
- 位于调式之前
- 位于文件名末尾
- 完全缺省

因此 BPM 应由 profile 指定位置；文件名解析应优先识别已发布的显式 token，并在内部 metadata 冲突时发出警告。

## 4. 对当前实现的重构要求

当前 `sample-pack` 工作流仍存在以下结构性问题：

1. 扫描默认包含预设、Artwork 和文档，缺少 Audio/MIDI/Preset/Artwork/Document 资源边界。
2. 解析器把 `CL_IE_`、`Shaw_Tyan_` 和 `BT_` 的身份 token 当成普通 `type/name`。
3. 当前模板强制使用 `meta_prefix + pack_name + large_category + small_category + detail + number`。
4. 当前自动编号会把每个不同原始 stem 视为独立分组，容易为所有文件生成 `_01`。
5. 当前 BPM 解析主要依赖末尾数字或显式 `BPM`，会漏掉中间 BPM。
6. 当前 `scale` 不能表达复杂和弦、双音、括号和转位。
7. 当前 action 把 BPM 追加在最终尾部，无法表示 Botanica 的 `BPM_调式_名称` 结构。
8. 当前快捷标签和大/小类别不足以覆盖真实资源词汇。
9. 扫描已发布采样包时，候选值不会自动形成可幂等的原始命名结构。

## 5. 建议的 profile 模型

```text
profile_id
delivery_type?
resource_type
resource_subtype?
prefix_fields[]
ordered_segments[]
optional_segments[]
variant_style?
asset_index_style?
fixed_prefix_tokens[]
fixed_suffix_tokens[]
```

示例：

```text
Botanica loop profile:
{pack_code}_{resource_type}_{bpm?}_{key_or_chord?}_{name}_{variant?}_{fixed_suffix?}

Shaw BASS profile:
{author_code}_{pack_code}_{resource_type}_{asset_index}_{name}_{key_or_chord?}

ColdLight MIDI profile:
{author_code}_{pack_code}_{resource_type}_{resource_subtype}_{name}_{key_or_chord?}_{bpm?}
```

这些只是从样本提炼的 profile 示例，不是要硬编码成唯一规则。

## 6. XLSX 词表范围

`sample-pack.xlsx` 只记录从样本中获得的结构性 token 和代表性语义 token，明确排除：

```text
包名值
作者/厂牌值
调式/和弦值
BPM 值
```

表格保留资源类型、子类型、交付类别、限定词、资产编号形式、变体形式、固定尾标形式和名称样例。作者后缀个案不作为通用规则写入词表。

## 7. 实施优先级

1. 增加资源类型过滤，默认只处理 Audio 和 MIDI。
2. 将作者简写、包名简写和全称元数据拆分，允许作者字段为空。
3. 引入按资源类型选择的 profile 模板。
4. 将 `asset_index`、`variant` 和 `collision_suffix` 分离。
5. 增加 `key_or_chord` 原始字段，支持完整音乐 token。
6. 改为 profile 感知的 BPM 解析与冲突提示。
7. 为三个真实样本包建立解析、重预览幂等性和非音频跳过测试。

